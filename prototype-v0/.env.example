# Local development only. Hosted production uses the secret of the same name
# in the existing Site's runtime environment. Never use a VITE_ prefix.
OJP_API_KEY=
# Optional local national timetable pilot; leave empty for live timetable only.
# Start tools/swiss_timetable.ts separately. Never use a VITE_ prefix.
SWISS_TIMETABLE_URL=
SWISS_TIMETABLE_TOKEN=
